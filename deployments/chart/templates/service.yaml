apiVersion: v1
kind: Service
metadata:
  name: {{ include "nocalhost-api.fullname" . }}
  labels:
    {{- include "nocalhost-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nocalhost-api.selectorLabels" . | nindent 4 }}
