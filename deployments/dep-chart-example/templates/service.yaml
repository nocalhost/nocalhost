apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo-bar.fullname" . }}
  labels:
    {{- include "foo-bar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foo-bar.selectorLabels" . | nindent 4 }}
